schema_version: 1
id: example-agent
name: Example Agent
description: Replace this with a short public summary.
version: 0.1.0
model:
  provider: openrouter
  name: gpt-5-mini
  temperature: 0.2
  max_tokens: 1800
runtime:
  timeout_seconds: 60
  internet_access: false
  execution_notes: Executes inside the shared AgentHub runtime.
io:
  input_mode: text
  output_mode: markdown
tools:
  enabled: false
  items: []
public_instructions: |
  Send a POST request to /api/agents/example-agent/execute with the user input as JSON.
